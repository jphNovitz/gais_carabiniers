{% set route = app.request.attributes.get('_route') %}
{% set active = 'font-semibold' %}
{% set inactive = 'hover:underline' %}

{% set clubName = globalInfos and globalInfos.name ? globalInfos.name : 'Club' %}

<header class="relative z-50">
    <nav class="bg-base-header text-base-header-content border-gray-200 px-4 lg:px-6 py-2.5">
        <div class="flex flex-wrap justify-between items-center mx-auto max-w-screen-xl relative">
            <a href="https://gc-bernissart.be" class="flex items-center">
                {% if globalInfos and globalInfos.logoName %}
                    <img src="/images/{{ globalInfos.logoName }}" class="inline-block mr-3 h-9 sm:h-12"
                         alt="{{ clubName }}"/>
                {% endif %}
                <span class="self-center text-xl lg:text-2xl font-teko font-teko leading-none tracking-wide">{{ clubName }}</span>
            </a>
            <details id="mobile-main-menu" class="group/mobile ml-auto lg:contents">
                <summary class="btn btn-circle btn-ghost swap swap-rotate list-none border-0 bg-transparent! shadow-none hover:bg-transparent! focus:bg-transparent! active:bg-transparent! lg:hidden [&amp;::-webkit-details-marker]:hidden"
                         aria-controls="mobile-menu-2">
                    <span class="sr-only">{% trans %}button.open_main_menu {% endtrans %}</span>
                    <svg class="col-start-1 row-start-1 h-8 w-8 transition-all duration-200 group-open/mobile:-rotate-45 group-open/mobile:opacity-0" fill="currentColor" xmlns="http://www.w3.org/2000/svg"
                         viewBox="0 0 512 512">
                        <path d="M64,384H448V341.33H64Zm0-106.67H448V234.67H64ZM64,128v42.67H448V128Z"/>
                    </svg>
                    <svg class="col-start-1 row-start-1 h-8 w-8 rotate-45 opacity-0 transition-all duration-200 group-open/mobile:rotate-0 group-open/mobile:opacity-100" fill="currentColor" xmlns="http://www.w3.org/2000/svg"
                         viewBox="0 0 512 512">
                        <polygon
                                points="400 145.49 366.51 112 256 222.51 145.49 112 112 145.49 222.51 256 112 366.51 145.49 400 256 289.49 366.51 400 400 366.51 289.49 256 400 145.49"/>
                    </svg>
                </summary>
            <div id="mobile-menu-2" class="absolute left-1/2 top-full w-screen -translate-x-1/2 bg-base-header px-4 pb-4 lg:static lg:order-1 lg:flex lg:w-auto lg:translate-x-0 lg:bg-transparent lg:p-0">
                <ul class="flex flex-col mt-4 font-medium lg:flex-row lg:space-x-8 lg:mt-0">
                    <li>
                        <a href="{{ path('app_home') }}"
                           class="{{ route == 'app_home' ? active : inactive }}"
                           aria-current="{{ route == 'app_home' ? 'page' : 'false' }}">
                            {% trans %}nav.home{% endtrans %}
                        </a>
                    </li>

                    <li>
                        <a href="{{ path('app_category_show', {'slug': 'stand-tir-hainaut'}) }}"
                           class="{{ route == 'app_category_show' ? active : inactive }}"
                           aria-current="{{ (route == 'app_category_show' and app.request.get('slug') == 'stand-tir-hainaut') ? 'page' : 'false' }}">
                            {% trans %}nav.about{% endtrans %}
                        </a>
                    </li>

                    <li>
                        <details id="competitions-menu" class="group relative" data-controller="navigation-dropdown"
                                 data-action="click@document->navigation-dropdown#closeOnOutsideClick keydown.esc@document->navigation-dropdown#close turbo:before-cache@document->navigation-dropdown#close">
                            <summary id="dropdown-standings"
                                     class="flex list-none items-center gap-1 cursor-pointer">
                                {% trans %}nav.competitions{% endtrans %}
                                <svg class="w-4 h-4 transition-transform group-open:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                          d="M19 9l-7 7-7-7"/>
                                </svg>
                            </summary>

                            <div id="dropdown-standings-menu"
                                 class="absolute left-0 top-full z-10 w-44 divide-y divide-gray-100 rounded-lg bg-base-header bg-opacity-70 text-base-header-content shadow dark:bg-gray-700">
                                <ul class="py-2 text-sm">
                                <li>
                                    {% set slug = app.request.attributes.get('slug') %}
                                    <a href="{{ path('app_category_show', {'slug': 'competitions-tir-sportif'}) }}"
                                       class="block px-4 py-2  {{ route == 'app_category_show' and slug=='competitions-tir-sportif' ? active : inactive }}"
                                    >
                                        {{ 'nav.presentation'|trans }}
                                    </a>
                                </li>

                                {% set isAgenda = route == 'app_agenda' %}
                                <li>
                                    <a href="{{ path('app_agenda') }}"
                                       class="block px-4 py-2 {{ route == 'app_agenda' ? active : inactive }}"
                                       aria-current="{{ isAgenda ? 'page' : 'false' }}">
                                        {{ 'nav.agenda'|trans }}
                                    </a>
                                </li>

                                <li>
                                    {% set category = app.request.attributes.get('category') %}
                                    <a href="{{ path('meeting_standing_category', {category: 'standard'}) }}"
                                       class="block px-4 py-2 {{ route == 'meeting_standing_category' and category == 'standard' ? active : inactive }}">
                                        {{ 'nav.standings.general'|trans }}
                                    </a>
                                </li>
                                <li>
                                    {% set category = app.request.attributes.get('category') %}
                                    <a href="{{ path('meeting_standing_category', {category: 'appuye'}) }}"
                                       class="block px-4 py-2 {{ route == 'meeting_standing_category' and category == 'appuye' ? active : inactive }}">
                                        {{ 'nav.standings.supported'|trans }}
                                    </a>
                                </li>

                                <li>
                                    <a href="{{ path('app_meeting_index') }}"
                                       class="block px-4 py-2 {{ route == 'app_meeting_index' ? active : inactive }}">
                                        {{ 'nav.standings.other'|trans }}
                                    </a>
                                </li>

                                </ul>
                            </div>
                        </details>
                    </li>

                    <li>
                        <a href="{{ path('app_faq') }}"
                           class="{{ route == 'app_faq' ? active : inactive }}"
                           aria-current="{{ route == 'app_faq' ? 'page' : 'false' }}">
                            {% trans %}nav.faq{% endtrans %}
                        </a>
                    </li>

                    <li>
                        <a href="{{ path('app_contact') }}"
                           class="{{ route == 'app_contact' ? active : inactive }}"
                           aria-current="{{ route == 'app_contact' ? 'page' : 'false' }}">
                            {% trans %}nav.contact{% endtrans %}
                        </a>
                    </li>

                </ul>

            </div>
            </details>
        </div>
    </nav>
</header>
