<button {{ attributes.defaults({
  'class': 'w-fit flex justify-center items-center bg-primary/90 dark:bg-primary/70 text-primary-content border hover:bg-primary focus:outline-none focus:ring-2 focus:ring-primary/30 font-medium rounded-lg text-sm px-5 py-2.5'
  })}} type="submit">
               {% block content %} {% endblock %}
</button>
