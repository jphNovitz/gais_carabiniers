framework:
    asset_mapper:
        # The paths to make available to the asset mapper.
        paths:
            - assets/
        excluded_patterns:
            - '*/.agents/*'
            - '*/.claude/*'
            - '*/skills-lock.json'
            - '*/fonts/Barlow/*.ttf'
            - '*:Zone.Identifier'
