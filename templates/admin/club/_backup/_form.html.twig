{{ form_start(form, {'attr' : {'class': 'my-8'}}) }}
    {{ form_widget(form) }}
<button class="text-primary-content bg-primary/90 hover:bg-primary/90 focus:outline-none focus:ring-4 focus:ring-primary/30 font-medium rounded-full text-sm px-5 py-2.5 text-center me-2 mb-2 dark:bg-primary/70 dark:hover:bg-primary/80 dark:focus:ring-primary/90  my-8">{{ button_label|default('Save') | trans}}</button>
{{ form_end(form) }}

