<form method="post" action="{{ path('admin_faq_delete', {'id': faq.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ faq.id) }}">
    <button class="btn">Supprimer</button>
</form> 